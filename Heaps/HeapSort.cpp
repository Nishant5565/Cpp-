#include <iostream>
using namespace std;

// ? What is Heap ? 
// * A heap is a complete binary tree, where every node has a value greater than or equal to its children.
// * A complete binary tree is a binary tree in which every level, except possibly the last, is completely filled, and all nodes are as far left as possible. Also the nodes at the last level are filled from left to right.



int main(){

}