#include <iostream>
#include <string>  //? Including this file to use string datatype.
using namespace std;

// ! First read about character Arrays then move to strings.

int main(){
     string str ;
     getline(cin,str); // * getline() is a function which is used to take input of a string with spaces.
     cout << str;


     // ! Some important methods on string.
      
     return 0;
     
}